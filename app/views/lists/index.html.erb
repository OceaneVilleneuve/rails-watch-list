<h1>Lists</h1>
<%@lists.each do |list| %>
<li><%=link_to list.name,
{:action => 'show', :id => list.id}%></li>
<%= link_to "Delete",
  list_path(list),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
<%end%>

<%=link_to "+ Add a list", {:action => 'new'}%>
